# RealCap Next.js Migration - Phase 4: SEO Fixes

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Address critical SEO issues from audit: remove fake aggregateRating schema, create FAQ page with FAQPage schema, create Compare pages, verify all footer links work, add remaining Solution pages, publish initial blog content.

**Architecture:** Fix schema markup to comply with Google guidelines, implement missing pages identified in SEO audit, ensure all internal links resolve.

**Tech Stack:** Next.js pages, Schema.org JSON-LD, MDX content

---

## Task 1: Remove Fake aggregateRating from All Pages

**Files:**
- Verify: `src/components/seo/SoftwareApplicationSchema.tsx`
- Verify: `src/app/[locale]/layout.tsx`

- [ ] **Step 1: Verify SoftwareApplicationSchema has no aggregateRating**

Read `src/components/seo/SoftwareApplicationSchema.tsx`:

Expected: Schema should NOT contain aggregateRating property

Verify the schema from Phase 2 is correct:

```typescript
const schema = {
  '@context': 'https://schema.org',
  '@type': 'SoftwareApplication',
  name: 'RealCap',
  applicationCategory: 'UtilitiesApplication',
  operatingSystem: 'Windows, macOS, Web',
  description: '...',
  offers: {
    '@type': 'Offer',
    price: '0',
    priceCurrency: 'USD',
  },
  // NO aggregateRating!
};
```

If aggregateRating exists, remove it.

- [ ] **Step 2: Search for any remaining aggregateRating in codebase**

Run: `grep -r "aggregateRating" src/`

Expected: No matches found

- [ ] **Step 3: Verify in build output**

Run: `npm run build && grep -r "aggregateRating" out/`

Expected: No aggregateRating in any HTML output

- [ ] **Step 4: Commit if changes needed**

If aggregateRating was found and removed:

```bash
git add -A
git commit -m "fix: remove fake aggregateRating schema (SEO audit fix)"
```

---

## Task 2: Create Full FAQ Page

**Files:**
- Create: `src/app/[locale]/faq/page.tsx`
- Create: `src/app/[locale]/faq/[topic]/page.tsx`
- Create: `src/components/faq/FAQItem.tsx`
- Create: `src/components/seo/FAQSchema.tsx`
- Modify: `content/en/faq/index.mdx`
- Modify: `content/zh/faq/index.mdx`

- [ ] **Step 1: Expand FAQ content**

Modify `content/en/faq/index.mdx`:

```mdx
---
title: "Frequently Asked Questions"
description: "Complete guide to RealCap screenshot verification FAQ"
topics:
  - slug: "screenshot-verification-basics"
    name: "Screenshot Verification Basics"
    questions:
      - question: "What is screenshot verification?"
        answer: "Screenshot verification confirms that a screenshot was captured authentically and has not been tampered with or generated by AI. RealCap creates an immutable authenticity record at the moment of capture."
      - question: "How does RealCap detect AI-generated fake screenshots?"
        answer: "RealCap captures metadata at the moment of screenshot capture, including device information, timestamp, and capture context. This metadata is encrypted and cannot be retroactively added to AI-generated images."
      - question: "Can RealCap verify screenshots taken before installation?"
        answer: "No. RealCap can only verify screenshots captured through our app, browser extension, or API. Screenshots taken before RealCap installation cannot be verified retroactively."
      - question: "What types of screenshots can RealCap verify?"
        answer: "RealCap can verify any screenshot captured through our capture system, including bank balance screenshots, income proof screenshots, identity documents, and communication records."
  - slug: "integration-questions"
    name: "Integration Questions"
    questions:
      - question: "How do I integrate RealCap API?"
        answer: "RealCap provides a RESTful API that can be integrated into any application. See our API documentation for endpoint details, request formats, and code examples."
      - question: "What platforms does RealCap support?"
        answer: "RealCap is available as a desktop application (Windows, macOS), browser extension (Chrome, Firefox), and mobile app (iOS, Android). We also provide SDKs for direct integration."
      - question: "Is there a webhook callback option?"
        answer: "Yes. RealCap supports webhook callbacks for verification completion. Configure your callback URL in the dashboard settings."
---

# Frequently Asked Questions

Find answers to all your questions about RealCap screenshot verification.
```

Modify `content/zh/faq/index.mdx`:

```mdx
---
title: "常见问题"
description: "RealCap截图验证常见问题完整指南"
topics:
  - slug: "screenshot-verification-basics"
    name: "截图验证基础"
    questions:
      - question: "什么是截图验证？"
        answer: "截图验证确认截图是真实拍摄的，未被篡改或由AI生成。RealCap在截图瞬间创建不可篡改的真实性记录。"
      - question: "RealCap如何检测AI生成的虚假截图？"
        answer: "RealCap在截图瞬间捕获元数据，包括设备信息、时间戳和截图上下文。这些元数据加密后无法事后添加到AI生成的图片中。"
      - question: "RealCap可以验证安装前拍摄的截图吗？"
        answer: "不可以。RealCap只能验证通过我们应用、浏览器扩展或API拍摄的截图。安装前拍摄的截图无法事后验证。"
      - question: "RealCap可以验证哪些类型的截图？"
        answer: "RealCap可以验证通过我们捕获系统拍摄的任何截图，包括银行余额截图、收入证明截图、身份文件和通讯记录。"
  - slug: "integration-questions"
    name: "集成问题"
    questions:
      - question: "如何集成RealCap API？"
        answer: "RealCap提供RESTful API，可集成到任何应用。查看我们的API文档获取端点详情、请求格式和代码示例。"
      - question: "RealCap支持哪些平台？"
        answer: "RealCap提供桌面应用（Windows、macOS）、浏览器扩展（Chrome、Firefox）和移动应用（iOS、Android）。我们还提供直接集成的SDK。"
      - question: "是否支持回调通知？"
        answer: "是的。RealCap支持验证完成的回调通知。在后台设置中配置您的回调URL。"
---

# 常见问题

查找关于RealCap截图验证的所有问题答案。
```

- [ ] **Step 2: Create FAQSchema component**

Create `src/components/seo/FAQSchema.tsx`:

```typescript
interface FAQQuestion {
  question: string;
  answer: string;
}

interface FAQSchemaProps {
  questions: FAQQuestion[];
}

export function FAQSchema({ questions }: FAQSchemaProps) {
  const schema = {
    '@context': 'https://schema.org',
    '@type': 'FAQPage',
    mainEntity: questions.map((q) => ({
      '@type': 'Question',
      name: q.question,
      acceptedAnswer: {
        '@type': 'Answer',
        text: q.answer,
      },
    })),
  };

  return (
    <script
      type="application/ld+json"
      dangerouslySetInnerHTML={{ __html: JSON.stringify(schema) }}
    />
  );
}
```

- [ ] **Step 3: Create FAQItem component**

Create `src/components/faq/FAQItem.tsx`:

```typescript
'use client';

import { useState } from 'react';
import { cn } from '@/lib/utils/cn';

interface FAQItemProps {
  question: string;
  answer: string;
}

export function FAQItem({ question, answer }: FAQItemProps) {
  const [isOpen, setIsOpen] = useState(false);

  return (
    <div className="border-b border-gray-200">
      <button
        onClick={() => setIsOpen(!isOpen)}
        className="w-full py-6 flex items-center justify-between text-left"
      >
        <span className="font-medium text-gray-900">{question}</span>
        <span className={cn('text-gray-500 transition-transform', isOpen && 'rotate-180')}>
          ▼
        </span>
      </button>
      {isOpen && (
        <div className="pb-6 text-gray-600">
          {answer}
        </div>
      )}
    </div>
  );
}
```

- [ ] **Step 4: Create FAQ index page**

Create `src/app/[locale]/faq/page.tsx`:

```typescript
import { unstable_setRequestLocale } from 'next-intl/server';
import { type Locale } from '@/i18n.config';
import { Container } from '@/components/ui/Container';
import { Link } from '@/components/ui/Link';
import { FAQItem } from '@/components/faq/FAQItem';
import { FAQSchema } from '@/components/seo/FAQSchema';
import { getFAQContent } from '@/lib/content/faq';

export default function FAQPage({ params: { locale } }: { params: { locale: Locale } }) {
  unstable_setRequestLocale(locale);
  
  const faqContent = getFAQContent(locale);
  const allQuestions = faqContent.frontmatter.questions || [];
  const topics = faqContent.frontmatter.topics || [];
  
  // Collect all questions from topics
  const topicQuestions = topics.flatMap((t) => t.questions || []);
  const displayQuestions = [...allQuestions, ...topicQuestions];
  const prefix = locale === 'en' ? '' : `/${locale}`;

  return (
    <>
      <FAQSchema questions={displayQuestions} />
      <Container size="md" className="py-12">
        <h1 className="text-3xl font-bold text-gray-900 mb-8">
          {locale === 'en' ? 'Frequently Asked Questions' : '常见问题'}
        </h1>

        {topics.length > 0 && (
          <div className="flex flex-wrap gap-4 mb-8">
            {topics.map((topic) => (
              <Link
                key={topic.slug}
                href={`${prefix}/faq/${topic.slug}`}
                className="px-4 py-2 bg-gray-100 rounded-full text-sm hover:bg-gray-200"
              >
                {topic.name}
              </Link>
            ))}
          </div>
        )}

        <div className="max-w-2xl">
          {displayQuestions.map((faq, index) => (
            <FAQItem key={index} question={faq.question} answer={faq.answer} />
          ))}
        </div>
      </Container>
    </>
  );
}
```

- [ ] **Step 5: Update FAQ content loader**

Modify `src/lib/content/faq.ts`:

```typescript
import fs from 'fs';
import path from 'path';
import matter from 'gray-matter';

interface FAQQuestion {
  question: string;
  answer: string;
}

interface FAQTopic {
  slug: string;
  name: string;
  questions: FAQQuestion[];
}

interface FAQContent {
  frontmatter: {
    title: string;
    description: string;
    questions: FAQQuestion[];
    topics: FAQTopic[];
  };
}

export function getFAQContent(locale: string): FAQContent {
  const filePath = path.join(process.cwd(), 'content', locale, 'faq', 'index.mdx');
  
  if (!fs.existsSync(filePath)) {
    return {
      frontmatter: {
        title: 'FAQ',
        description: 'Common questions',
        questions: [],
        topics: [],
      },
    };
  }
  
  const fileContent = fs.readFileSync(filePath, 'utf-8');
  const { data } = matter(fileContent);
  
  return {
    frontmatter: {
      title: data.title,
      description: data.description,
      questions: data.questions || [],
      topics: data.topics || [],
    },
  };
}

export function getFAQTopic(locale: string, slug: string): FAQTopic | null {
  const faqContent = getFAQContent(locale);
  return faqContent.frontmatter.topics.find((t) => t.slug === slug) || null;
}
```

- [ ] **Step 6: Create FAQ topic page**

Create `src/app/[locale]/faq/[topic]/page.tsx`:

```typescript
import { unstable_setRequestLocale } from 'next-intl/server';
import { type Locale } from '@/i18n.config';
import { Container } from '@/components/ui/Container';
import { FAQItem } from '@/components/faq/FAQItem';
import { FAQSchema } from '@/components/seo/FAQSchema';
import { getFAQTopic } from '@/lib/content/faq';
import { notFound } from 'next/navigation';

interface FAQTopic {
  slug: string;
  name: string;
  questions: Array<{ question: string; answer: string }>;
}

export function generateStaticParams() {
  const locales: Locale[] = ['en', 'zh'];
  const topics = ['screenshot-verification-basics', 'integration-questions', 'pricing-questions'];
  
  return locales.flatMap((locale) =>
    topics.map((topic) => ({
      locale,
      topic,
    }))
  );
}

export default function FAQTopicPage({
  params: { locale, topic },
}: {
  params: { locale: Locale; topic: string };
}) {
  unstable_setRequestLocale(locale);
  
  const topicContent = getFAQTopic(locale, topic);
  
  if (!topicContent) {
    notFound();
  }
  
  return (
    <>
      <FAQSchema questions={topicContent.questions} />
      <Container size="md" className="py-12">
        <h1 className="text-3xl font-bold text-gray-900 mb-8">
          {topicContent.name}
        </h1>
        <div className="max-w-2xl">
          {topicContent.questions.map((faq, index) => (
            <FAQItem key={index} question={faq.question} answer={faq.answer} />
          ))}
        </div>
      </Container>
    </>
  );
}
```

- [ ] **Step 7: Commit FAQ page**

```bash
git add content/en/faq/ content/zh/faq/ src/lib/content/faq.ts src/components/faq/ src/components/seo/FAQSchema.tsx src/app/[locale]/faq/
git commit -m "feat: add complete FAQ page with FAQPage schema (fixes footer broken link)"
```

---

## Task 3: Create Compare Pages

**Files:**
- Create: `src/app/[locale]/compare/page.tsx`
- Create: `src/components/compare/CompareTable.tsx`

- [ ] **Step 1: Create compare index page**

Create `src/app/[locale]/compare/page.tsx`:

```typescript
import { unstable_setRequestLocale } from 'next-intl/server';
import { type Locale } from '@/i18n.config';
import { Container } from '@/components/ui/Container';
import { Link } from '@/components/ui/Link';

export default function CompareIndexPage({ params: { locale } }: { params: { locale: Locale } }) {
  unstable_setRequestLocale(locale);
  const prefix = locale === 'en' ? '' : `/${locale}`;

  const comparisons = [
    {
      slug: 'background-check-services',
      title: locale === 'en' ? 'RealCap vs Background Check Services' : 'RealCap vs背调公司',
      description: locale === 'en' 
        ? 'Compare screenshot verification with traditional background check approaches'
        : '比较截图验证与传统背调方法',
    },
    {
      slug: 'manual-vs-automated',
      title: locale === 'en' ? 'Manual vs Automated Verification' : '人工vs自动验证',
      description: locale === 'en'
        ? 'Compare manual screenshot review with RealCap automated verification'
        : '比较人工截图审核与RealCap自动验证',
    },
  ];

  return (
    <Container size="lg" className="py-12">
      <h1 className="text-3xl font-bold text-gray-900 mb-4">
        {locale === 'en' ? 'RealCap Comparisons' : 'RealCap对比分析'}
      </h1>
      <p className="text-lg text-gray-600 mb-8">
        {locale === 'en'
          ? 'See how RealCap compares to alternative approaches'
          : '了解RealCap与替代方案的对比'}
      </p>

      <div className="grid grid-cols-1 md:grid-cols-2 gap-8">
        {comparisons.map((comp) => (
          <Link
            key={comp.slug}
            href={`${prefix}/compare/${comp.slug}`}
            className="block bg-white rounded-lg p-6 shadow-sm border border-gray-200 hover:shadow-md"
          >
            <h2 className="text-xl font-semibold text-gray-900 mb-2">
              {comp.title}
            </h2>
            <p className="text-gray-600">
              {comp.description}
            </p>
          </Link>
        ))}
      </div>
    </Container>
  );
}
```

- [ ] **Step 2: Create CompareTable component**

Create `src/components/compare/CompareTable.tsx`:

```typescript
'use client';

import { cn } from '@/lib/utils/cn';

interface CompareRow {
  feature: string;
  realcap: string;
  alternative: string;
}

interface CompareTableProps {
  rows: CompareRow[];
  alternativeName: string;
}

export function CompareTable({ rows, alternativeName }: CompareTableProps) {
  return (
    <div className="overflow-x-auto">
      <table className="w-full border-collapse">
        <thead>
          <tr className="bg-gray-50">
            <th className="px-4 py-3 text-left font-medium text-gray-900">Feature</th>
            <th className="px-4 py-3 text-left font-medium text-blue-600">RealCap</th>
            <th className="px-4 py-3 text-left font-medium text-gray-600">{alternativeName}</th>
          </tr>
        </thead>
        <tbody>
          {rows.map((row, index) => (
            <tr key={index} className="border-b border-gray-200">
              <td className="px-4 py-3 text-gray-900">{row.feature}</td>
              <td className={cn(
                'px-4 py-3',
                row.realcap === '✓' ? 'text-green-600 font-medium' : 'text-gray-600'
              )}>
                {row.realcap}
              </td>
              <td className={cn(
                'px-4 py-3',
                row.alternative === '✓' ? 'text-green-600' : 
                row.alternative === '✗' ? 'text-red-600' : 'text-gray-600'
              )}>
                {row.alternative}
              </td>
            </tr>
          ))}
        </tbody>
      </table>
    </div>
  );
}
```

- [ ] **Step 3: Create compare topic page**

Create `src/app/[locale]/compare/[topic]/page.tsx`:

```typescript
import { unstable_setRequestLocale } from 'next-intl/server';
import { type Locale } from '@/i18n.config';
import { Container } from '@/components/ui/Container';
import { CompareTable } from '@/components/compare/CompareTable';

export default function CompareTopicPage({
  params: { locale, topic },
}: {
  params: { locale: Locale; topic: string };
}) {
  unstable_setRequestLocale(locale);

  // Static comparison data for now
  const comparisons = {
    'manual-vs-automated': {
      title: locale === 'en' ? 'Manual vs Automated Verification' : '人工vs自动验证',
      alternativeName: locale === 'en' ? 'Manual Review' : '人工审核',
      rows: [
        { feature: locale === 'en' ? 'Speed' : '速度', realcap: '<1s', alternative: 'Minutes' },
        { feature: locale === 'en' ? 'Accuracy' : '准确率', realcap: '99.9%', alternative: 'Variable' },
        { feature: locale === 'en' ? 'AI Detection' : 'AI检测', realcap: '✓', alternative: '✗' },
        { feature: locale === 'en' ? 'Batch Processing' : '批量处理', realcap: '✓', alternative: '✗' },
        { feature: locale === 'en' ? 'Cost per Verification' : '每次验证成本', realcap: '$0.01', alternative: '$5+' },
        { feature: locale === 'en' ? '24/7 Available' : '全天候可用', realcap: '✓', alternative: '✗' },
      ],
    },
    'background-check-services': {
      title: locale === 'en' ? 'RealCap vs Background Check Services' : 'RealCap vs背调公司',
      alternativeName: locale === 'en' ? 'Background Check' : '背调公司',
      rows: [
        { feature: locale === 'en' ? 'Screenshot Focus' : '截图专项', realcap: '✓', alternative: '✗' },
        { feature: locale === 'en' ? 'Real-time Verification' : '实时验证', realcap: '✓', alternative: '✗' },
        { feature: locale === 'en' ? 'AI Detection' : 'AI检测', realcap: '✓', alternative: 'Limited' },
        { feature: locale === 'en' ? 'API Integration' : 'API集成', realcap: '✓', alternative: '✗' },
        { feature: locale === 'en' ? 'Cost' : '成本', realcap: 'Low', alternative: 'High' },
      ],
    },
  };

  const comparison = comparisons[topic as keyof typeof comparisons];

  if (!comparison) {
    return (
      <Container size="lg" className="py-12">
        <h1 className="text-3xl font-bold text-gray-900">
          {locale === 'en' ? 'Comparison Not Found' : '对比页面不存在'}
        </h1>
      </Container>
    );
  }

  return (
    <Container size="lg" className="py-12">
      <h1 className="text-3xl font-bold text-gray-900 mb-8">
        {comparison.title}
      </h1>
      <CompareTable rows={comparison.rows} alternativeName={comparison.alternativeName} />
    </Container>
  );
}
```

- [ ] **Step 4: Commit compare pages**

```bash
git add src/app/[locale]/compare/ src/components/compare/
git commit -m "feat: add compare pages with comparison tables"
```

---

## Task 4: Create Remaining Solution Pages (MCN, Matchmaking, Gaming, Rental)

**Files:**
- Create: `content/en/solutions/mcn.mdx`
- Create: `content/zh/solutions/mcn.mdx`
- Create: `content/en/solutions/matchmaking.mdx`
- Create: `content/zh/solutions/matchmaking.mdx`
- Create: `content/en/solutions/gaming.mdx`
- Create: `content/zh/solutions/gaming.mdx`
- Create: `content/en/solutions/rental.mdx`
- Create: `content/zh/solutions/rental.mdx`

- [ ] **Step 1: Create MCN solution content**

Create `content/en/solutions/mcn.mdx`:

```mdx
---
title: "MCN Agency Screenshot Verification"
description: "RealCap helps MCN agencies verify influencer income claims and prevent signing fraud"
industry: "mcn"
keywords: ["mcn verification", "influencer fraud", "income proof fake", "signing scam"]
targetAudience: "MCN agency talent management teams"
painPoints:
  - "Influencers forge income screenshots to inflate their value"
  - "AI-generated fake engagement proof is increasingly common"
  - "False income claims lead to overvalued contracts"
features:
  - "Income screenshot verification API"
  - "Engagement proof authenticity detection"
  - "Contract value assessment support"
  - "Fraud pattern analysis"
relatedArticles: ["mcn-signing-fraud-case"]
pricingLink: "/pricing"
apiDocsLink: "/docs/api/verify"
---

# MCN Agency Screenshot Verification

RealCap helps MCN agencies verify influencer income and engagement screenshots before signing contracts, preventing overvalued deals and fraud losses.
```

Create `content/zh/solutions/mcn.mdx`:

```mdx
---
title: "MCN机构截图验证方案"
description: "RealCap帮助MCN机构验证网红收入声明，防止签约欺诈"
industry: "mcn"
keywords: ["MCN验证", "网红欺诈", "收入证明造假", "签约诈骗"]
targetAudience: "MCN机构艺人管理团队"
painPoints:
  - "网红伪造收入截图夸大身价"
  - "AI生成的虚假互动证明越来越常见"
  - "虚假收入声明导致合同估值过高"
features:
  - "收入截图验证API"
  - "互动证明真实性检测"
  - "合同价值评估支持"
  - "欺诈模式分析"
relatedArticles: ["mcn-signing-fraud-case"]
pricingLink: "/pricing"
apiDocsLink: "/docs/api/verify"
---

# MCN机构截图验证方案

RealCap帮助MCN机构在签约前验证网红收入和互动截图，防止估值过高的合同和欺诈损失。
```

- [ ] **Step 2: Create Matchmaking solution content**

Create `content/en/solutions/matchmaking.mdx`:

```mdx
---
title: "Dating Platform Screenshot Verification"
description: "RealCap helps dating platforms verify user identity and financial claims for safer matchmaking"
industry: "matchmaking"
keywords: ["dating verification", "identity fraud", "romance scam", "financial proof fake"]
targetAudience: "Dating platform trust and safety teams"
painPoints:
  - "Users forge identity documents to create fake profiles"
  - "Financial screenshots are used to deceive potential partners"
  - "Romance scams exploit fake wealth claims"
features:
  - "Identity document verification"
  - "Financial screenshot authenticity check"
  - "Profile verification badge"
  - "Fraud alert system"
relatedArticles: []
pricingLink: "/pricing"
apiDocsLink: "/docs/api/verify"
---

# Dating Platform Screenshot Verification

RealCap helps dating platforms verify user identity and financial screenshots, creating safer matchmaking experiences and preventing romance scams.
```

Create `content/zh/solutions/matchmaking.mdx`:

```mdx
---
title: "相亲平台截图验证方案"
description: "RealCap帮助相亲平台验证用户身份和财务声明，保障安全相亲"
industry: "matchmaking"
keywords: ["相亲验证", "身份欺诈", "情感诈骗", "财务证明造假"]
targetAudience: "相亲平台信任与安全团队"
painPoints:
  - "用户伪造身份文件创建虚假资料"
  - "财务截图用于欺骗潜在伴侣"
  - "情感诈骗利用虚假财富声明"
features:
  - "身份文件验证"
  - "财务截图真实性检查"
  - "资料验证徽章"
  - "欺诈预警系统"
relatedArticles: []
pricingLink: "/pricing"
apiDocsLink: "/docs/api/verify"
---

# 相亲平台截图验证方案

RealCap帮助相亲平台验证用户身份和财务截图，创造更安全的相亲体验并防止情感诈骗。
```

- [ ] **Step 3: Create Gaming solution content**

Create `content/en/solutions/gaming.mdx`:

```mdx
---
title: "Gaming Trade Screenshot Verification"
description: "RealCap helps gaming platforms verify asset ownership and transaction screenshots"
industry: "gaming"
keywords: ["gaming verification", "asset fraud", "trade scam", "ownership proof"]
targetAudience: "Gaming platform trade security teams"
painPoints:
  - "Players forge asset ownership screenshots"
  - "Transaction history is manipulated"
  - "In-game currency screenshots are fake"
features:
  - "Asset ownership verification"
  - "Transaction screenshot check"
  - "Currency balance verification"
  - "Trade fraud detection"
relatedArticles: []
pricingLink: "/pricing"
apiDocsLink: "/docs/api/verify"
---

# Gaming Trade Screenshot Verification

RealCap helps gaming platforms verify in-game asset ownership and transaction screenshots, preventing trade fraud and ensuring secure transactions.
```

Create `content/zh/solutions/gaming.mdx`:

```mdx
---
title: "游戏交易截图验证方案"
description: "RealCap帮助游戏平台验证资产所有权和交易截图"
industry: "gaming"
keywords: ["游戏验证", "资产欺诈", "交易诈骗", "所有权证明"]
targetAudience: "游戏平台交易安全团队"
painPoints:
  - "玩家伪造资产所有权截图"
  - "交易历史被篡改"
  - "游戏货币截图是假的"
features:
  - "资产所有权验证"
  - "交易截图检查"
  - "货币余额验证"
  - "交易欺诈检测"
relatedArticles: []
pricingLink: "/pricing"
apiDocsLink: "/docs/api/verify"
---

# 游戏交易截图验证方案

RealCap帮助游戏平台验证游戏内资产所有权和交易截图，防止交易欺诈并确保安全交易。
```

- [ ] **Step 4: Create Rental solution content**

Create `content/en/solutions/rental.mdx`:

```mdx
---
title: "Rental Agency Screenshot Verification"
description: "RealCap helps rental agencies verify tenant financial documents and identity proofs"
industry: "rental"
keywords: ["rental verification", "tenant fraud", "income proof fake", "identity verification"]
targetAudience: "Rental agency property management teams"
painPoints:
  - "Tenants forge income proof screenshots"
  - "Identity documents are manipulated"
  - "Bank balance screenshots are AI-generated"
features:
  - "Income proof verification API"
  - "Identity document check"
  - "Bank balance verification"
  - "Tenant fraud detection"
relatedArticles: []
pricingLink: "/pricing"
apiDocsLink: "/docs/api/verify"
---

# Rental Agency Screenshot Verification

RealCap helps rental agencies verify tenant financial screenshots and identity documents, preventing rental fraud and ensuring trustworthy tenants.
```

Create `content/zh/solutions/rental.mdx`:

```mdx
---
title: "租房中介截图验证方案"
description: "RealCap帮助租房中介验证租户财务文件和身份证明"
industry: "rental"
keywords: ["租房验证", "租户欺诈", "收入证明造假", "身份验证"]
targetAudience: "租房中介物业管理团队"
painPoints:
  - "租户伪造收入证明截图"
  - "身份文件被篡改"
  - "银行余额截图由AI生成"
features:
  - "收入证明验证API"
  - "身份文件检查"
  - "银行余额验证"
  - "租户欺诈检测"
relatedArticles: []
pricingLink: "/pricing"
apiDocsLink: "/docs/api/verify"
---

# 租房中介截图验证方案

RealCap帮助租房中介验证租户财务截图和身份文件，防止租房欺诈并确保可靠租户。
```

- [ ] **Step 5: Commit remaining solution pages**

```bash
git add content/en/solutions/ content/zh/solutions/
git commit -m "feat: add remaining 4 solution pages (MCN, Matchmaking, Gaming, Rental)"
```

---

## Task 5: Create MCN and Placeholder Case Articles

**Files:**
- Create: `content/en/blog/cases/mcn-signing-fraud-case.mdx`
- Create: `content/zh/blog/cases/mcn-signing-fraud-case.mdx`

- [ ] **Step 1: Create MCN case article**

Create `content/en/blog/cases/mcn-signing-fraud-case.mdx`:

```mdx
---
title: "MCN Agency Influencer Signing Fraud Case"
description: "How an MCN agency prevented $500K in fraudulent contract losses using RealCap verification"
category: "cases"
industry: ["mcn"]
keywords: ["mcn fraud case", "influencer scam", "income fake", "contract fraud"]
author: "RealCap Team"
publishDate: "2026-04-28"
lastModified: "2026-04-28"
readingTime: 8
featured: true
image: "/images/blog/mcn-signing-fraud-case.png"
tags: ["MCN案例", "签约欺诈", "网红验证"]
relatedArticles: []
cta:
  text: "Learn MCN Solution"
  link: "/solutions/mcn"
---

# MCN Agency Influencer Signing Fraud Case

An MCN agency was about to sign an influencer claiming $100K monthly income. RealCap verification revealed the income screenshots were AI-generated, saving the agency $500K in contract losses.

## The Situation

An influencer approached the MCN agency with screenshots showing impressive monthly income from multiple platforms. The agency was ready to offer a lucrative contract based on this proof.

## The Red Flags

- Income screenshots had inconsistent formatting
- Platform logos appeared slightly altered
- Timestamps didn't match typical payment cycles

## The Verification

RealCap's verification API flagged the screenshots as AI-generated, detecting metadata inconsistencies and image artifacts typical of AI forgery.

## The Outcome

The agency avoided signing a fraudulent contract worth $500K. They now require RealCap verification for all influencer income claims.

[Learn more about MCN solutions](/solutions/mcn)
```

Create `content/zh/blog/cases/mcn-signing-fraud-case.mdx`:

```mdx
---
title: "MCN机构网红签约欺诈案例"
description: "某MCN机构使用RealCap验证避免50万美元虚假签约损失"
category: "cases"
industry: ["mcn"]
keywords: ["MCN欺诈案例", "网红诈骗", "收入造假", "合同欺诈"]
author: "RealCap Team"
publishDate: "2026-04-28"
lastModified: "2026-04-28"
readingTime: 8
featured: true
image: "/images/blog/mcn-signing-fraud-case.png"
tags: ["MCN案例", "签约欺诈", "网红验证"]
relatedArticles: []
cta:
  text: "了解MCN方案"
  link: "/zh/solutions/mcn"
---

# MCN机构网红签约欺诈案例

某MCN机构差点签约一名声称月收入10万美元的网红。RealCap验证揭示收入截图是AI生成的，避免了50万美元的合同损失。

## 情况背景

一名网红向MCN机构展示多平台月收入截图，机构准备基于此证明提供高额签约合同。

## 发现异常

- 收入截图格式不一致
- 平台logo略有改动
- 时间戳与典型支付周期不匹配

## 验证过程

RealCap验证API标记截图为AI生成，检测到AI伪造典型的元数据不一致和图像痕迹。

## 最终结果

机构避免了价值50万美元的虚假签约。现在所有网红收入声明都要求RealCap验证。

[了解更多MCN方案](/zh/solutions/mcn)
```

- [ ] **Step 2: Commit MCN case article**

```bash
git add content/en/blog/cases/ content/zh/blog/cases/
git commit -m "feat: add MCN influencer signing fraud case article"
```

---

## Task 6: Verify All Footer Links Work

- [ ] **Step 1: Build the project**

Run: `npm run build`

Expected: Build succeeds

- [ ] **Step 2: Check footer link files exist**

Run: `ls out/privacy/index.html && ls out/terms/index.html && ls out/about/index.html && ls out/faq/index.html`

Expected: All footer link pages exist

- [ ] **Step 3: Check Chinese versions**

Run: `ls out/zh/privacy/index.html && ls out/zh/terms/index.html && ls out/zh/about/index.html && ls out/zh/faq/index.html`

Expected: All Chinese footer link pages exist

- [ ] **Step 4: If any missing, create the page**

If any footer link returns 404, create the corresponding page in previous tasks.

---

## Task 7: Update Sitemap with All New Pages

- [ ] **Step 1: Verify sitemap generation**

Run: `npm run build && cat out/sitemap.xml`

Expected: Sitemap includes all pages for both locales

- [ ] **Step 2: Check sitemap page count**

Run: `grep -c "<url>" out/sitemap.xml`

Expected: ≥30 URLs (homepage, blog, solutions, docs, pricing, about, privacy, terms, faq, compare for both locales)

---

## Task 8: Final SEO Verification

- [ ] **Step 1: Verify no fake aggregateRating**

Run: `grep -r "aggregateRating" out/`

Expected: No matches

- [ ] **Step 2: Verify FAQPage schema exists**

Run: `grep "@type.*FAQPage" out/faq/index.html`

Expected: FAQPage schema found

- [ ] **Step 3: Verify Article schema for blog**

Run: `grep "@type.*Article" out/blog/fraud-detection/detect-bank-balance-fraud/index.html`

Expected: Article schema found

- [ ] **Step 4: Verify Product schema for solutions**

Run: `grep "@type.*Product" out/solutions/lending/index.html`

Expected: Product schema found

- [ ] **Step 5: Verify hreflang for all pages**

Run: `grep "hreflang" out/index.html | head -5`

Expected: hreflang for en, zh, x-default present

- [ ] **Step 6: Create Phase 4 completion commit**

```bash
git add -A
git commit -m "feat: complete Phase 4 - SEO fixes, FAQ page, Compare pages, all 5 Solutions"
```

---

## Verification Checklist

- [ ] No aggregateRating in any page
- [ ] FAQ page with FAQPage schema
- [ ] Compare pages created
- [ ] All 5 Solution pages exist (Lending, MCN, Matchmaking, Gaming, Rental)
- [ ] All footer links resolve (Privacy, Terms, About, FAQ)
- [ ] Blog articles: 2 fraud detection + 2 cases
- [ ] Article schema for each blog article
- [ ] Product schema for each solution
- [ ] Sitemap includes all new pages
- [ ] hreflang tags on all pages